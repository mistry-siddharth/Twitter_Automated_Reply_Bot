import stream, datetime

def run(): #Calls Script stream and the main function in it which starts the bot pre-processing stage
	stream.main()

while 1!=0: #Makes sure the bot is always running
	run()
